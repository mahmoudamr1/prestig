{% if categories and categories.size > 0 %}
<div class="ab-categories-section">
  <header class="ab-categories-header">
    <p class="ab-categories-kicker">{{ theme_data.categories_header_subtitle | default: 'best seller collections' }}</p>
    <h2 class="ab-categories-title">{{ theme_data.categories_header_title | default: 'Shop by Category' }}</h2>
  </header>
  <div class="ab-categories">
    <div class="ab-categories-grid">
      {% for category in categories %}
        <a class="ab-cat-tile" href="/collections/{{ category.slug }}">
          <span class="ab-cat-tile__media">
            <img class="ab-cat-tile__img" src="{{ category.thumb }}" alt="" loading="lazy" />
            <span class="ab-cat-tile__dim"></span>
          </span>
          <span class="ab-cat-tile__content">
            <span class="ab-cat-tile__title">{{ category.name }}</span>
          </span>
        </a>
      {% endfor %}
    </div>
  </div>
</div>
{% endif %}
